/*
Le plus petit nombre dois etre au sommet a la fin du trie

creation checker; + visualizateur de push 

/// PROCESSE DE CONFECTION ET ALGO
    - initialiser les tab et regarder si il n'est pas = NULL; et prend la taille du tableau;
/all:while(ce n'est pas rangé du < au > )
    - check les possition de chaque index du tableau, pour derterminer les nombre qui a deplacer ;
    - regarder les prés move pour trouver le move le plus rentable (nbc minimume pour un placement parfait au cote de nbr < &| >)
    - executer le fonction de move
    end

//LILSTE A PENSER VERIF BUG ETC

verifier si le move sont possible au niveau des contrainte(tableau vide ou elements manquans,le nbr de move pixel etc...)



*/